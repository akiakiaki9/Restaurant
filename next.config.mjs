/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['127.0.0.1', 'restaurant-booking-system-production.up.railway.app'], // Добавляем новый домен
    },
};

export default nextConfig;